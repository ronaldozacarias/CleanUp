package br.com.cleanUp.repository;

import java.util.Date;
import java.util.List;

import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.CrudRepository;


//public interface ServicoRepository extends CrudRepository<Servico, Integer> {
////	@Query(value = "SELECT s FROM servico AS u WHERE diarista = ? AND  data > CURRENT_DATE()")
////	List<Servico> findByDiarista(Integer codigo, Date data);
//}
